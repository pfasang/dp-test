type Project {
  id: ID! @id
  name: String! @unique
  description: String!
  manager: ID!
  startDate: DateTime!
  endDate: DateTime
  activities: [Activity!]! @relation(name: "ActivityProject", onDelete: CASCADE)
}
