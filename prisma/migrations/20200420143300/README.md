# Migration `20200420143300`

This migration has been generated by Patrik Fašang at 4/20/2020, 2:33:00 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "default$default"."ActivitySkill" DROP CONSTRAINT IF EXiSTS "ActivitySkill_skillId_fkey",
ALTER COLUMN "skillId" DROP NOT NULL;

ALTER TABLE "default$default"."UserSkill" DROP CONSTRAINT IF EXiSTS "UserSkill_ownerId_fkey",
DROP CONSTRAINT IF EXiSTS "UserSkill_skillId_fkey",
ALTER COLUMN "ownerId" DROP NOT NULL,
ALTER COLUMN "skillId" DROP NOT NULL;

ALTER TABLE "default$default"."ActivitySkill" ADD FOREIGN KEY ("skillId")REFERENCES "default$default"."Skill"("id") ON DELETE SET NULL  ON UPDATE CASCADE

ALTER TABLE "default$default"."UserSkill" ADD FOREIGN KEY ("skillId")REFERENCES "default$default"."Skill"("id") ON DELETE SET NULL  ON UPDATE CASCADE

ALTER TABLE "default$default"."UserSkill" ADD FOREIGN KEY ("ownerId")REFERENCES "default$default"."Profile"("user") ON DELETE SET NULL  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200420142934-test2..20200420143300
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 model Activity {
   id            String          @id @default(cuid())
@@ -23,10 +23,10 @@
   id        String    @id @default(cuid())
   level     Int
   owner     Activity @relation(fields: [ownerId], references: [id])
   ownerId   String
-  skill     Skill    @relation(fields: [skillId], references: [id])
-  skillId   String
+  skill     Skill?    @relation(fields: [skillId], references: [id])
+  skillId   String?
 }
 model Profile {
   user      String     @id
@@ -59,9 +59,9 @@
 model UserSkill {
   id      String   @id @default(cuid())
   level   Int
-  skill   Skill   @relation(fields: [skillId], references: [id])
-  skillId String
-  owner   Profile @relation(fields: [ownerId], references: [user])
-  ownerId String
+  skill   Skill?   @relation(fields: [skillId], references: [id])
+  skillId String?
+  owner   Profile? @relation(fields: [ownerId], references: [user])
+  ownerId String?
 }
```


